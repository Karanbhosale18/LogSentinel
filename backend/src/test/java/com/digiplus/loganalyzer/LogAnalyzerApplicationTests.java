package com.digiplus.loganalyzer;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/** Verifies the full Spring context (beans, JPA, config binding) wires up. */
@SpringBootTest
class LogAnalyzerApplicationTests {

    @Test
    void contextLoads() {
        // If the application context fails to start, this test fails.
    }
}
